apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.7
version = '1.0'
jar {
  manifest {
    attributes 'Implementation-Title' : 'JUnit-Sandbox', 'Implementation-Version' : version
  }
}

repositories {
  mavenCentral()
}

dependencies {
  testCompile(  [ group: 'com.googlecode.openpojo', name: 'openpojo', version: '0.6.2' ],
                [ group: 'junit', name: 'junit', version: '4.9+' ] )
}
